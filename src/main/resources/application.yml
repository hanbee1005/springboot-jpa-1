spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/Dev/study/BackEnd/Spring/jpashop/jpashop;MVCC=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true  ## System.out 으로 로그 보이게 하는 것 (운영 사용 X)
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
